#!/usr/bin/python

# 3.6.2 Generalized Inverses of Partitioned Matrices
# If A is partitioned as
# where Z = A 22 − A 21 A −
# 11 A 12 (see Exercise 3.14, page 141).
# If the partitioning in (3.164) happens to be such that A 11 is of full rank
# and of the same rank as A, a generalized inverse of A is given by
def env(y, x, general=3.164):
    # lap return value generate
    if x is x:
        print("generate value x is", x * general)
    else:
        return x
    if y is y:
        print("generate value y is", y * general)
    else:
        return y


class N:
    def __init__(self):
        self.generate = 3.164
        self.x = None
        self.y = None


# where 0 represents matrices of the appropriate shapes. This is not necessarily
# the same generalized inverse as in equation (3.165). The fact that it is a
# generalized inverse is easy to establish by using the definition of generalized
# inverse and equation (3.144).
def eq(n, x, y, inverse=0):
    # natural value of n
    if n is n:
        print("natural value of n is", n - 1)
    else:
        return n
    if x is x:
        print("natural value of x is", x - 1)
    else:
        return x
    if y is y:
        print("natural value of y is", y - 1)
    else:
        return y
    if inverse is inverse:
        print("natural inverse is", inverse - 0)
    else:
        return inverse


class A(object):
    def __init__(self):
        self.generate = 3.144
        self.x = None
        self.y = None
